rootProject.name = 'olo-be'

// Build olo-core dist (JARs + catalog JSON) as part of syncExtensionCatalog when present.
includeBuild('../../olo-mono/olo-core') {
    dependencySubstitution {
        substitute module('org.olo:olo-core') using project(':core')
    }
}
